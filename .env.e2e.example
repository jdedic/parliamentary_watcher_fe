# Copy this file to .env.e2e and fill in the values before running e2e tests.
# .env.e2e is gitignored — never commit real credentials.

# URL of the test FastAPI backend (must use a separate test database)
TEST_API_URL=http://localhost:8001/api/v1

# API key for the test backend
TEST_API_KEY=parliai3290-afae
